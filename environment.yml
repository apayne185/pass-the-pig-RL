name: ptp-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - numpy
  - pygame
  - pytorch
  - tensorboard
  - numba
  - pip
  - pip:
      - gymnasium==0.26.2
      - gym-notices
